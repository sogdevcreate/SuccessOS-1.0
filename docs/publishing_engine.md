# Publishing Engine

Publishing is provider-neutral and operates only on an approved final render. It validates final-master provenance, rights, metadata, platform limits, scheduling, disclosures, and compatible providers. Missing providers return an explicit unavailable state; no URLs, uploads, or external IDs are fabricated.
